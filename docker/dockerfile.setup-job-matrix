FROM python:3.12

RUN --mount=target=/requirements/requirements.python.txt,type=bind,source=requirements/requirements.python.txt <<EOF
    set -ex
    pip install -r /requirements/requirements.python.txt
EOF
